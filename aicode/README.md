# DOM Action Recorder Chrome Extension

This extension records DOM/user actions, sends them to Grak AI, and generates Playwright TypeScript code.

## Setup
- Load the folder as an unpacked extension in Chrome.
- Enter your Grak AI API key in the options page.

## Features
- Records clicks and other actions
- Sends actions to Grak AI
- Displays generated Playwright code
